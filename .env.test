# Test Environment Configuration
# This file is used by integration tests

# API Configuration
API_BASE_URL=http://localhost:3000

# Test Tokens (optional - for authenticated tests)
# Set these to run authenticated integration tests
# TEST_DEVICE_TOKEN=your-test-token-here
# TEST_EXPIRED_TOKEN=expired-token-for-testing

# Database Configuration (optional - for direct DB tests)
# SUPABASE_URL=your-test-supabase-url
# SUPABASE_ANON_KEY=your-test-anon-key

# Test Mode
NODE_ENV=test
